---
const base = import.meta.env.BASE_URL;
const defaultLang = 'es';
const redirectUrl = `${base}${defaultLang}/`.replace(/\/+/g, '/');
---
<meta http-equiv="refresh" content={`0;url=${redirectUrl}`} />